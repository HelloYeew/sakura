<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup Label="Project">
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>disable</ImplicitUsings>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <NoWarn>$(NoWarn);CS1591;NU5104</NoWarn>
    <DefineConstants>$(DefineConstants);JETBRAINS_ANNOTATIONS</DefineConstants>
  </PropertyGroup>
  <PropertyGroup Label="Nuget">
    <Title>Sakura Framework</Title>
    <PackageId>Sakura.Framework</PackageId>
    <PackageReleaseNotes>Automated build</PackageReleaseNotes>
    <PackageTags>sakura framework</PackageTags>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="JetBrains.Annotations" Version="2024.3.0" />
    <PackageReference Include="ManagedBass" Version="3.1.1" />
    <PackageReference Include="ManagedBass.Fx" Version="3.1.1" />
    <PackageReference Include="ManagedBass.Mix" Version="3.1.1" />
    <PackageReference Include="NUnit" Version="4.3.2" />
    <PackageReference Include="Silk.NET.OpenGL" Version="2.22.0" />
    <PackageReference Include="Silk.NET.SDL" Version="2.22.0" />
    <!-- TODO: PackageReference to NativeLibs when published to NuGet-->
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\Sakura.Framework.SourceGenerators\Sakura.Framework.SourceGenerators.csproj" ReferenceOutputAssembly="false" OutputItemType="Analyzer" />
  </ItemGroup>
  <ItemGroup>
    <None Include="runtimes\**\native\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>
  <PropertyGroup>
<!--    Path where the source generator should emit its generated files. -->
    <SakuraGenerator_EnableSourceDebugging>true</SakuraGenerator_EnableSourceDebugging>
    <SakuraGenerator_EmitSourcePath>$(MSBuildProjectDirectory)/SourceGenerated</SakuraGenerator_EmitSourcePath>
  </PropertyGroup>
  <ItemGroup>
    <CompilerVisibleProperty Include="SakuraGenerator_EnableSourceDebugging" />
    <CompilerVisibleProperty Include="SakuraGenerator_EmitSourcePath" />
  </ItemGroup>
  <ItemGroup>
    <!--    Prevent the generated files from being included in the project as normal source files. -->
    <Compile Remove="$(SakuraGenerator_EmitSourcePath)/**/*.cs" />
  </ItemGroup>
</Project>
