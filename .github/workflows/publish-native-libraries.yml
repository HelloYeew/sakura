name: Publish Sakura.Framework.NativeLibraries to NuGet

on:
  workflow_dispatch:
    inputs:
      patch:
        description: 'Patch number (the last segment of YYYY.MMDD.patch)'
        required: false
        default: '0'

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    name: Pack and Publish
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Compute CalVer version
        id: version
        run: |
          YEAR=$(date -u +'%Y')
          MM=$(date -u +'%-m')
          DD=$(date -u +'%d')
          PATCH=${{ github.event.inputs.patch }}
          VERSION="${YEAR}.${MM}${DD}.${PATCH}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Pack
        run: |
          dotnet pack Sakura.Framework.NativeLibraries/Sakura.Framework.NativeLibraries.csproj \
            --configuration Release \
            -p:Version=${{ steps.version.outputs.version }} \
            --output ./nupkg

      - name: NuGet login (Trusted Publishing)
        uses: NuGet/login@v1
        id: login
        with:
          user: ${{ secrets.NUGET_USER }}

      - name: Publish to nuget.org
        run: |
          dotnet nuget push ./nupkg/*.nupkg \
            --api-key ${{ steps.login.outputs.NUGET_API_KEY }} \
            --source https://api.nuget.org/v3/index.json \
            --skip-duplicate

      - name: Upload nupkg artifact
        uses: actions/upload-artifact@v4
        with:
          name: nupkg
          path: ./nupkg/*.nupkg
